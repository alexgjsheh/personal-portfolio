import SiteLogo from "./SiteLogo";
import Nav from "./Nav";

const Header = () => {
    return (
        <header id="home">
            <SiteLogo />
            <Nav />
        </header>
    );
};

export default Header;
